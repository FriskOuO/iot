# 🚦 超聲波感測器動態警示系統

## 功能說明

現在 `useUltrasonicSensor` Hook 已經整合了動態變色警示功能，能夠根據測量到的距離自動調整警示等級和顏色！

## 📊 警示等級

### 🟢 安全 (Safe) - 綠色
- **距離範圍**: > 150cm
- **顏色代碼**: `#00ff00` (綠色)
- **標籤**: ✅ 安全
- **說明**: 距離還很遠

### 🟡 小心 (Caution) - 黃色
- **距離範圍**: 80-150cm
- **顏色代碼**: `#ffff00` (黃色)
- **標籤**: 👀 小心
- **說明**: 請注意距離

### 🟠 注意 (Warning) - 橙色
- **距離範圍**: 30-80cm
- **顏色代碼**: `#ff8800` (橙色)
- **標籤**: ⚡ 注意
- **說明**: 越來越近了

### 🔴 危險 (Danger) - 紅色
- **距離範圍**: ≤ 30cm
- **顏色代碼**: `#ff0000` (紅色)
- **標籤**: ⚠️ 危險
- **說明**: 快撞到了！

## 🎯 實現的動態效果

### 1. **警示顯示框**
- 背景色會隨著警示等級動態變化
- 邊框顏色同步更新
- 帶有光暈效果 (box-shadow)
- 平滑過渡動畫 (transition)

### 2. **距離數值**
- 文字顏色與警示等級同步
- 字體加粗並放大以提高可見性
- 邊框左側顯示警示色條

### 3. **即時圖表**
- 線條顏色隨著當前警示等級改變
- 點的顏色也會同步更新
- 背景填充色帶有透明度

### 4. **警示說明圖例**
- 顯示四個等級的完整說明
- 包含顏色圓點、距離範圍和說明文字
- 危險等級帶有脈動動畫

## 🔧 技術實現

### Hook 返回值增強

```javascript
{
  sensorData: {
    measuredDistance: number,    // 測量距離
    warningLevel: string,        // 警示等級: 'safe' | 'caution' | 'warning' | 'danger'
    warningColor: string,        // 警示顏色 (HEX)
    warningLabel: string,        // 警示標籤 (含 emoji)
    warningDescription: string,  // 警示說明
    // ... 其他原有屬性
  }
}
```

### 警示計算邏輯

```javascript
const calculateWarningLevel = (distance) => {
  if (distance <= 30) return { level: 'danger', color: '#ff0000', ... };
  if (distance <= 80) return { level: 'warning', color: '#ff8800', ... };
  if (distance <= 150) return { level: 'caution', color: '#ffff00', ... };
  return { level: 'safe', color: '#00ff00', ... };
};
```

## 📱 使用效果

### 情境 1: 車輛進場
```
距離: 200cm → 綠色 (安全)
距離: 120cm → 黃色 (小心)
距離: 60cm  → 橙色 (注意)
距離: 20cm  → 紅色 (危險) + 系統提示停車
```

### 情境 2: 即時反饋
- 玩家移動車輛時，儀表板會即時更新顏色
- 圖表線條顏色也會動態變化
- 提供直觀的視覺反饋，就像真實倒車雷達

## 🎮 遊戲整合

這個警示系統現在完全整合到：
- ✅ `useUltrasonicSensor` Hook
- ✅ `IoTDashboard` 組件
- ✅ 即時圖表顯示
- ✅ MQTT 日誌記錄

## 🎨 視覺特效

1. **顏色過渡**: 使用 CSS `transition` 實現平滑的顏色變化
2. **光暈效果**: 使用 `box-shadow` 創造警示氛圍
3. **脈動動畫**: 危險等級的圓點會持續閃爍
4. **即時同步**: 所有視覺元素同步更新

## 🚀 測試方式

1. 啟動遊戲
2. 進入停車場景
3. 觀察儀表板上的警示顯示
4. 移動車輛接近障礙物
5. 看到顏色從綠→黃→橙→紅的動態變化

## 📖 教育價值

這個功能展示了：
- **物理模擬**: 超聲波測距原理
- **即時反饋**: 感測器數據到視覺化的過程
- **IoT 應用**: 類似真實的停車輔助系統
- **數據視覺化**: 如何將抽象數值轉換為直觀警示

---

**完成！** 現在您的 IoT 遊戲具有完整的動態警示系統，讓玩家能夠直觀地感受到虛擬數據與視覺畫面的同步連動！ 🎉
